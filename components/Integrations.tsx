export function Integrations() {
  return (
    <section className="relative overflow-hidden bg-gradient-to-br from-[#EAF4FF] to-[#E7FBF6] px-6 py-40">
      <h2 className="relative z-10 mx-auto max-w-4xl text-center text-5xl font-medium">
        Pam integrates with the systems your dealership already runs on.
      </h2>

      <div className="absolute inset-0">
        {/* dotted paths + floating logos */}
      </div>
    </section>
  );
}
